[build]
  functions = "netlify/functions"
  publish = "."

[build.environment]
  GIT_SUBMODULE_STRATEGY = "none"

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
```

Guarda con **Ctrl+S**. Luego en el terminal:
```
git add .
git commit -m "fix submodule"
git push